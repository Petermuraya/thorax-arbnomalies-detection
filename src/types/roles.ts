// Define Role type
export type Role = "patient" | "healthstaff" | "admin";

// Optional: Export a constant list of roles (for forms, selects, etc.)
export const ROLES: Role[] = ["patient", "healthstaff", "admin"];
